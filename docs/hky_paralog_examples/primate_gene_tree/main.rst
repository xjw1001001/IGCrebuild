primate HKY85
=============

This example just uses PAML to estimate parameters
of an HKY85 process and compute a log likelihood,
for a small alignment and phylogenetic tree.

Newick tree:

.. literalinclude:: for.paml.newick.tree
   :linenos:

Fasta file:

.. literalinclude:: for.paml.ECP_EDN.fasta
   :linenos:

Baseml control file:

.. literalinclude:: baseml.ctl
   :linenos:

Output file (mlb):

.. literalinclude:: mlb
   :linenos:
